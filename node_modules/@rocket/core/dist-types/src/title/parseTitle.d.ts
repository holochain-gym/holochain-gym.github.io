/** @typedef {import('./types').EleventyPage} EleventyPage */
/**
 * Parses a title and extracts the relevante data for it.
 * A title can contain
 * - ">>" to define a parent => child relationship
 * - "||" to define the order for this page
 *
 * @example
 * Foo ||3
 * Foo >> Bar ||10
 *
 * @param {string} inTitle
 * @return {EleventyPage}
 */
export function parseTitle(inTitle: string): EleventyPage;
export type EleventyPage = import("./types").EleventyPage;
//# sourceMappingURL=parseTitle.d.ts.map